<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link href="https://fonts.googleapis.com/css?family=Rubik:400,700,900&display=swap" rel="stylesheet">
	<link rel="stylesheet" type="text/css" href="/css/trix.css">
	<link rel="stylesheet" type="text/css" href="/css/app.css">
	<title>{{nombrePagina}}</title>
</head>
<body>
	<!--para renderizar codigo de javascript en la plantilla html se usa el binding {{variable}} -->
	<div class="nombre-sitio contenedor">
		<h1>
			<a href="/">DevJobs</a>
		</h1>

		{{#if barra}}<!-- Si la variable barra esta en true se ejecuta el codigo de adentro-->
			<div class="buscador">
				<form action="#">
					<input type="text" class="buscar" placeholder="Buscar empleos">
					<input type="submit" value="Buscar">
				</form>
			</div>

		{{/if}}<!--fin del if-->
	</div>
	<header class="site-header contenedor separador">
		<h2>{{nombrePagina}}</h2>

		{{#if tagline}}
			<p class="tagline">{{tagline}}</p>
		{{/if}}

		{{#if boton}}
			<a href="/vacantes/nueva" class="btn btn-azul">Publicar vacante</a>
		{{/if}}
	
	</header>

	<div class="contenido-principal contenedor">
		{{#if mensajes}}
			<div class="alertas">
				{{#mostrarAlertas mensajes}}
				{{/mostrarAlertas }}
			</div>
		{{/if}}
		<!--para renderizar codigo html de otra plantilla se usa el binding {{{variable}}} -->
		{{{body}}}
	</div>

	<script src="/js/trix.js"></script>
	<script src="/dist/bundle.js"></script><!-- cargar este archivo en el masterpage para que web pack funcione en el cliente-->
</body>
</html>